/*
  Declare a variable.
  The keyword var followed by the name of the variable.
  Then, after the assignment operator, is the value the variable will contain
*/

var name = prompt('What is your name?');

console.log(name);

console.log('Hello World from ' + name);
